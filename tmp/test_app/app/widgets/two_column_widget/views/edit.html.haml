%h4
  Column 1 width

= cms_tag(:div, @widget, :column_1_width, data: { editor: 'slider', min: 1, max: 12 })

%h4
  Column 2 width

= cms_tag(:div, @widget, :column_2_width, data: { editor: 'slider', min: 1, max: 12 })
