%h4
  Headline

= cms_tag(:div, @obj, :headline)

%h4
  Error not found page

= cms_edit_reference(@obj, :error_not_found_page, data: { filter: 'error_pages' })

%h4
  Locale

= cms_tag(:div, @obj, :locale)
